# AGENTS.md

## 저장소 목적

이 저장소는 Quartz v4로 구축한 개인 기술 블로그이자 프로젝트 포트폴리오입니다.
데이터 엔지니어링 경험과 AI/ML 학습·프로젝트 과정을 공개 가능한 근거를 바탕으로 기록합니다.

이 파일을 포함한 저장소 내용은 외부에 공개될 수 있습니다. 개인 작업 환경에만 필요한 설정이나 민감한 정보는 이 파일에 기록하지 않습니다.

## 주요 경로

- `content/projects/`: 직접 수행한 프로젝트와 포트폴리오 글
- `content/blog/`: 기술 글, 학습 회고, 생각 정리
- `content/notes/`: 주제별 학습 노트
- `content/templates/`: 발행하지 않는 문서 템플릿
- `quartz.config.ts`: 사이트 제목, 언어, 테마, 플러그인 설정
- `quartz.layout.ts`: 페이지 구성 요소와 배치
- `quartz/`: Quartz 본체 코드
- `docs/`: Quartz 원본 문서

`content/`를 사용자 작성 영역으로 봅니다. 명시적인 기능·디자인 변경 요청이 없다면 `quartz/`와 `docs/`의 원본 코드를 수정하지 않습니다.

## 작업 원칙

1. 작업 전에 관련 글과 설정을 읽고 현재 상태를 확인합니다.
2. 새 작업은 1~2문장으로 범위를 제안하고 승인받은 뒤 수정합니다.
3. 새 문서를 만들 때는 먼저 목차와 섹션별 요약을 제시합니다.
4. 요청 범위 밖의 글 일괄 수정이나 구조 개편은 실행하지 않고 제안만 합니다.
5. 저장소에서 확인할 수 없는 경력, 역할, 수치, 운영 결과를 추측해서 작성하지 않습니다.
6. 기존 사용자 변경 사항을 보존하고, 관련 없는 파일은 수정하지 않습니다.

개인 저장소의 기본 브랜치는 `main`을 사용합니다. Quartz 원본 업데이트 대상인 `upstream/v4`와 개인 저장소 브랜치를 혼동하지 않습니다.

## 프로젝트 글 작성 기준

프로젝트 글은 단순한 기술 목록보다 다음 질문에 답해야 합니다.

- 어떤 배경에서 무슨 문제를 해결했는가?
- 본인이 맡은 역할과 직접 수행한 범위는 무엇인가?
- 어떤 대안을 검토했고, 왜 해당 기술이나 구조를 선택했는가?
- 구현 중 어떤 문제가 발생했고 어떻게 진단·해결했는가?
- 결과를 어떤 명령, 테스트, 로그, 측정값 또는 산출물로 검증했는가?
- 성과, 한계, 후속 과제는 무엇인가?

계획과 실제 결과를 혼동하지 않도록 다음 상태를 구분합니다.

- `계획`: 아직 구현하거나 검증하지 않은 내용
- `구현 완료`: 코드나 문서를 작성한 상태
- `로컬 검증`: 로컬 환경에서 테스트 또는 빌드한 상태
- `운영 검증`: 실제 운영 환경에서 동작을 확인한 상태

검증하지 않은 내용을 완료된 성과처럼 표현하지 않습니다. 정량적 성과는 측정 기준과 근거가 있을 때만 사용합니다.

새 프로젝트 글은 `content/templates/project.md`를 출발점으로 사용합니다. 모든 항목을 억지로 채우지 말고, 프로젝트를 이해하고 평가하는 데 필요한 내용만 남깁니다.

## 기존 글 검토 및 재발행

기존 글은 공개 품질 검토가 끝날 때까지 `draft: true`를 유지합니다. 원문을 삭제하거나 일괄 재작성하지 않고 한 편씩 검토합니다.

- 기존 글의 작성 시점, 의미와 Git 이력을 보존합니다.
- 파일 이동이나 이름 변경으로 기존 URL을 깨뜨리지 않습니다.
- 형식 정리와 사실·내용 개정을 분리합니다.
- 오래된 정보는 현재 기준으로 재검증하고, 검증할 수 없으면 `확인 필요`로 표시합니다.
- 프로젝트 글에서 기존 글을 관련 학습 기록으로 연결할 수 있지만, 프로젝트 성과의 근거로 자동 간주하지 않습니다.
- 원문 출처, 작성 권한, 민감 정보, 깨진 링크와 이미지부터 확인합니다.
- 검토와 빌드가 끝난 글만 `draft: false`로 변경합니다.
- 대규모 일괄 변경 전에 대표 글 한 편으로 형식과 품질 기준을 확인합니다.

## 공개 정보 보호

글을 작성하거나 수정하기 전에 공개 가능한 정보인지 확인합니다.

- API 키, 비밀번호, 토큰, 인증서, 실제 환경 파일을 포함하지 않습니다.
- 고객명, 내부 시스템명, 서버 주소, 계정, 사내 URL, 데이터베이스 접속 정보는 공개하지 않습니다.
- 회사 코드나 문서를 원문 그대로 복사하지 않습니다.
- 업무 프로젝트는 필요한 경우 이름, 수치, 구조, 화면, 로그를 익명화합니다.
- 공개 범위가 불분명하면 임의로 작성하지 않고 사용자에게 확인합니다.

## 문서 형식

- 모든 텍스트 파일은 UTF-8로 저장합니다.
- 본문은 한국어를 기본으로 하고, 전문 용어는 필요한 경우 영어 원문을 처음 한 번 병기합니다.
- 불확실한 내용은 `추정` 또는 `확인 필요`로 표시합니다.
- 새 게시물에는 최소한 `title`, `date`, `tags`, `draft` 앞부분 메타데이터(frontmatter)를 둡니다.
- 날짜는 `YYYY-MM-DD` 형식을 사용합니다.
- 공개 준비가 끝나기 전에는 `draft: true`를 유지합니다.
- 제목 단계는 문서 제목 이후 `##`부터 순서대로 사용합니다.
- 코드 블록에는 가능한 한 언어를 지정하고, 실행 결과와 예시 입력을 구분합니다.
- 외부 자료를 참고한 경우 글 끝에 출처와 링크를 남깁니다.
- 내부 링크와 이미지 경로는 상대 경로를 사용하고 빌드 결과에서 깨짐 여부를 확인합니다.

기존 글의 형식이 새 기준과 다르더라도 요청 없이 전체를 일괄 변경하지 않습니다. 새 글과 직접 수정하는 글부터 기준을 적용합니다.

## 검증

변경 범위에 맞춰 다음 명령을 실행합니다.

- 콘텐츠만 변경: `npx quartz build`
- TypeScript·Quartz 설정 변경: `npm run check`, `npm test`, `npx quartz build`

검증 실패 시 변경으로 인한 문제인지 기존 문제인지 구분합니다. 현재 환경에서 검증할 수 없다면 실행하지 않은 항목과 이유를 명시합니다.
